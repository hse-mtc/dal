setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

run:
  web: uvicorn dal.asgi:application --host=0.0.0.0 --port=${PORT}

release:
  image: web
  command:
    - python manage.py migrate
