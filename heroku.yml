setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

run:
  web: uvicorn dms.asgi:backend

release:
    image: web
    command: >
      sh -c "python manage.py makemigrations &&
             python manage.py migrate"
