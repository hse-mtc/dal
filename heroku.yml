setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

run:
  web: uvicorn dms.asgi:application --host=0.0.0.0 --port=${PORT}

release:
    image: web
    command:
      - python manage.py makemigrations && python manage.py migrate
