setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    backend: Dockerfile
    frontend: frontend/Dockerfile

run:
  backend: gunicorn djangoherokuapp.wsgi --log-file -

release:
    image: 
      - backend
      - frontend
    command:
      - python manage.py migrate